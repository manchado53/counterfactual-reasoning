"""
FrozenLake state manager tests for the legacy Gymnasium pipeline.

Skipped: StateManagerFactory was removed; these tests cover infrastructure
that is no longer active.
"""
import pytest
pytest.skip("Legacy StateManagerFactory removed", allow_module_level=True)
